add_subdirectory( vm_api4c )
add_subdirectory( vm_wasm )
if (APPLE)
# add_subdirectory( vm_wasm4py )
endif()
add_subdirectory( wasm2c_contracts )
add_subdirectory( wasm2c_exports )
add_subdirectory( vm_micropython )
add_subdirectory( vm_lua )
